# Otter 配置(复制为 .env 并填入你的 key;查找顺序:环境变量 > ./.env > ~/.otter/.env)
# OpenAI 兼容端点(DeepSeek / OpenAI / Qwen / Ollama 同协议)
OTTER_BASE_URL=https://api.deepseek.com
OTTER_API_KEY=sk-你的key
OTTER_MODEL=deepseek-v4-flash
# 可选:单次任务最大步数(默认 30)
# OTTER_MAX_STEPS=30
